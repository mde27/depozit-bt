# Local / CI hints (Cloudflare secrets are set via wrangler / dashboard)
JWT_SECRET=change-me-to-a-long-random-secret

# After: wrangler d1 create depozit-bt
# paste database_id into wrangler.toml [[d1_databases]]
