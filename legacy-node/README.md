# Legacy Node (Express + better-sqlite3)

This folder is the **previous** single-process Node deployment. It is **not** used for Cloudflare Pages.

Prefer the root project: Vite client → `dist/` + `functions/` Pages Functions + D1.

Kept only for reference / offline VPS if needed.
